<h2>New Book</h2>

<%= form_for(@book) do |f| %>

	<h5>Title</h5>
	<div class="field">
	  <%= f.text_field :title %>
	</div>

	<h5>Body</h5>
	<div class="text">
	<%= f.text_area :body %>
    </div>

    <div class="action">
	  <%= f.submit 'Create Book' %>
    </div>

<% end %>